---
import Section from "@/layouts/Section.astro";
---

<Section>
    <div>
        <div
            id="drawer-swipe"
            class="fixed z-40 max-w-screen-lg mx-2 lg:mx-auto overflow-y-auto rounded-t-lg bg-gray-700 transition-transform bottom-0 left-0 right-0 translate-y-full bottom-[30px]"
            tabindex="-1"
            aria-labelledby="drawer-swipe-label"
        >
            <div
                class="p-4 cursor-pointer hover:bg-gray-300 dark:hover:bg-gray-700"
                data-drawer-toggle="drawer-swipe"
            >
                <span
                    class="absolute w-8 h-1 -translate-x-1/2 bg-gray-300 rounded-lg top-3 left-1/2"
                ></span>
            </div>
            <div class="max-w-screen-sm mx-auto">
                <!--                 <form class="mb-10 max-xl:px-4">
                    <h2 class="py-10 text-lg text-white text-center">
                        Para más información, rellena el siguiente formulario:
                    </h2>
                    <div class="mb-6">
                        <label for="email"></label>
                        <input
                            type="email"
                            id="email"
                            class="border border-gray-300 text-gray-300 text-sm rounded-lg focus:ring-primary focus:border-primary block w-full p-2.5"
                            placeholder="nombre@empresa.com"
                            required
                        />
                    </div>
                    <div class="mb-6">
                        <label for="subject"></label>
                        <input
                            type="text"
                            id="subject"
                            class="border border-gray-300 text-gray-300 text-sm rounded-lg focus:ring-primary focus:border-primary block w-full p-2.5"
                            placeholder="Asunto"
                            required
                        />
                    </div>
                    <div class="mb-6">
                        <label for="message"></label>
                        <textarea
                            id="message"
                            rows="2"
                            class="block p-2.5 w-full text-sm text-gray-300 rounded-lg border border-gray-300 focus:ring-primary focus:border-primary"
                            placeholder="Escribe tu mensaje"></textarea>
                    </div>
                    <button
                        type="submit"
                        class="w-fit hover:cursor-pointer transition duration-300 ease-in hover:border-primary hover:scale-95 text-white bg-primary border-1 border-white font-medium rounded-lg text-sm px-5 py-2.5 mb-2 block"
                        >Enviar mensaje</button
                    >
                </form> -->

                <form
                    class="mb-10 max-xl:px-4"
                    action="/actions/send-email"
                    method="post"
                >
                    <h2 class="py-10 text-lg text-white text-center">
                        Para más información, rellena el siguiente formulario:
                    </h2>
                    <div class="mb-6">
                        <label for="send-email__recipient"></label>
                        <input
                            placeholder="nombre@empresa.com"
                            class="border border-gray-300 text-gray-300 text-sm rounded-lg focus:ring-primary focus:border-primary block w-full p-2.5"
                            type="email"
                            name="recipient"
                            id="send-email__recipient"
                            required
                        />
                    </div>
                    <div class="mb-6">
                        <label for="send-email__subject"></label>
                        <input
                            placeholder="Asunto"
                            class="border border-gray-300 text-gray-300 text-sm rounded-lg focus:ring-primary focus:border-primary block w-full p-2.5"
                            type="text"
                            name="subject"
                            id="send-email__subject"
                            required
                        />
                    </div>
                    <div class="mb-6">
                        <label for="send-email__message"></label>
                        <textarea
                            placeholder="Escribe tu mensaje"
                            rows="2"
                            class="block p-2.5 w-full text-sm text-gray-300 rounded-lg border border-gray-300 focus:ring-primary focus:border-primary"
                            name="message"
                            id="send-email__message"
                            required></textarea>
                    </div>
                    <input
                        class="w-fit hover:cursor-pointer transition duration-300 ease-in hover:border-primary hover:scale-95 text-white bg-primary border-1 border-white font-medium rounded-lg text-sm px-5 py-2.5 mb-2 block"
                        type="submit"
                        value="Send message"
                    />
                </form>
            </div>
        </div>
    </div>
</Section>

<script>
    import { drawerToggle } from "@/assets/js/drawer";
    document.addEventListener("astro:page-load", drawerToggle);
</script>
